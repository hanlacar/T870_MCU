# T870 MCU — 안 될 때 보는 문서

**증상을 찾아서 그 줄만 읽으면 됩니다.** 위에서부터 자주 나는 순서입니다.

터미널에 뜬 메시지는 **자르지 말고 통째로** SJ 한테 보내주세요.
어느 줄이 진짜 원인인지는 그 앞뒤에 있습니다.

---

## 0. 뭘 해도 안 될 때 — 이것부터

```bash
cd ~/T870_MCU && ./실행.sh 점검
```

환경 문제 대부분이 여기서 잡힙니다. 이걸 먼저 돌리고 결과를 보내주세요.

---

# A. 설치 · 빌드가 안 될 때

## A-1. `AMENT_TRACE_SETUP_FILES: unbound variable`

```
/opt/ros/jazzy/setup.bash: line 8: AMENT_TRACE_SETUP_FILES: unbound variable
✗ 빌드 실패
```

**원인** — 배포판이 v11 이하입니다. 스크립트가 `set -u` 를 켠 채로 ROS 를
불러서 죽습니다. ROS 의 `setup.bash` 는 정의되지 않은 변수를 참조합니다.

**해결** — **v12 이상으로 다시 받으세요.** 스크립트 버그이지 PC 문제가 아닙니다.

---

## A-2. `colcon build` 가 패키지 이름 중복으로 통째로 실패

```
Multiple packages found with the same name "t870_mcu"
```

**원인** — 백업 폴더나 압축 풀어둔 사본을 colcon 이 같이 잡았습니다.

**확인**

```bash
find ~ -maxdepth 5 -type d -name t870_mcu 2>/dev/null
```

**해결**

```bash
touch ~/t870_backup_*/COLCON_IGNORE
```

워크스페이스 **안에** 사본이 또 있으면 그 상위 폴더에도 만들어 주세요.

```bash
touch <그폴더>/COLCON_IGNORE
```

---

## A-3. `설치.sh` 를 git 클론 안에서 돌렸을 때

```
⚠ 여기는 git 저장소다
```

**원인** — `설치.sh` 는 **zip 을 받은 분용**입니다. git 으로 받으면 클론 자체가
워크스페이스라, 설치를 돌리면 `~/T870_MCU` 라는 **두 번째 워크스페이스**가
생겨서 어느 쪽이 도는지 모르게 됩니다.

**해결** — git 으로 받았으면 빌드만 하세요.

```bash
cd ~/mcu_ws && source /opt/ros/jazzy/setup.bash && colcon build --symlink-install
```

---

## A-4. ROS 2 를 못 찾는다

```
✗ ROS 2 를 못 찾았다 (/opt/ros/*/setup.bash 없음)
```

**확인**

```bash
ls /opt/ros/
```

**해결** — Jazzy 설치가 안 됐거나 다른 위치입니다. 나온 경로를 알려주세요.

---

# B. 파이썬 · 실행이 안 될 때

## B-1. `ModuleNotFoundError: No module named 'serial'`

**원인 ①** — 프롬프트 앞에 `(venv)` 가 붙어 있으면 가상환경 안입니다.
그 안에는 pyserial 이 없습니다.

```bash
deactivate
```

또는 시스템 파이썬을 직접 지정합니다.

```bash
/usr/bin/python3 tools/run_v4_0830.py 5 2
```

**원인 ②** — pyserial 자체가 없습니다.

```bash
sudo apt install python3-serial
```

---

## B-2. 도구를 복사했는데 옛날 게 돈다

브라우저가 같은 이름을 `파일(1).py` 로 저장해서 옛 파일을 복사한 경우입니다.

**확인**

```bash
ls -t ~/Downloads/<파일이름>*.py
```

**해결** — 제일 최신 것을 복사합니다.

```bash
cp "$(ls -t ~/Downloads/<파일이름>*.py | head -1)" ~/T870_MCU/tools/<파일이름>.py
```

배포되는 도구는 **이름에 버전과 날짜가 붙습니다**(`run_v4_0830.py`).
이름이 다르면 다른 파일입니다.

---

# C. 시리얼 포트가 안 잡힐 때

## C-1. `Device or resource busy` / 브릿지가 `exit code 1` 로 죽는다

**시리얼 포트는 한 번에 한 프로그램만 씁니다.**

**확인**

```bash
pgrep -af "mcu_bridge|mcu_manager|drive_wasd|run_v4|push_v1|center_v2|serial_console"
```

**해결**

```bash
pkill -f "mcu_bridge|mcu_manager|drive_wasd|run_v4|push_v1|center_v2|serial_console"
sleep 1
```

그래도 안 되면 **아두이노 IDE 의 시리얼 모니터가 열려 있는지** 확인하세요.
그게 제일 흔합니다. 마지막 수단:

```bash
sudo fuser -k /dev/ttyACM*
```

---

## C-2. 포트를 아예 못 찾는다

```
포트를 못 찾았다. USB 확인.
```

**확인**

```bash
ls -l /dev/t870_mcu /dev/serial/by-id/ /dev/ttyACM* 2>&1
python3 tools/check_ports.py
```

| 결과 | 해결 |
|---|---|
| `/dev/ttyACM0` 는 있는데 `/dev/t870_mcu` 가 없음 | udev 규칙 미설치. `./setup.sh` 실행 |
| `ttyACM` 도 없음 | USB 케이블 / 아두이노 전원 확인. 케이블이 충전전용인 경우가 있음 |

---

## C-3. `Permission denied`

```bash
groups | grep dialout || sudo usermod -aG dialout $USER
```

**추가 후 로그아웃했다 다시 로그인해야 적용됩니다.**

---

## C-4. STATUS 가 한 줄도 안 온다

```
STATUS 응답 없음 (10초)
```

**펌웨어는 `S` 를 보내야만 STATUS 를 줍니다** (자동 발행 기본 꺼짐).
도구들은 알아서 물어보므로, 그래도 무응답이면:

1. 위 C-1 (다른 프로그램이 포트 점유)
2. 아두이노 전원 — USB 만으로는 모터가 안 돕니다. **24V 배터리 확인**
3. 케이블 불량

---

# D. 노드끼리 안 보일 때

## D-1. `ros2 topic list` 에 상대 토픽이 없다

**ROS 2 는 `ROS_DOMAIN_ID` 나 `RMW_IMPLEMENTATION` 이 하나만 달라도
노드끼리 서로 존재조차 모릅니다. 에러도 안 납니다.**

**확인**

```bash
echo "DOMAIN=$ROS_DOMAIN_ID  RMW=$RMW_IMPLEMENTATION"
```

**팀 합의값**

```
ROS_DOMAIN_ID=77
RMW_IMPLEMENTATION=rmw_fastrtps_cpp
```

**해결** — 그 터미널에서 먼저 이걸 부릅니다.

```bash
source ~/T870_MCU/t870_env.sh
```

다른 워크스페이스(`mmission_ws` 등)를 띄우는 터미널에서도 **같은 값**을 써야 합니다.

> ⚠ `~/.bashrc` 에 직접 적지 마세요. 나중에 값을 바꿀 때 그것만 남아서
> 혼자 다른 도메인에 떠 있게 됩니다.
> ```bash
> grep -n 'ROS_DOMAIN_ID\|RMW_IMPLEMENTATION' ~/.bashrc
> ```
> 나오면 그 줄을 지우세요.

---

## D-2. 토픽 이름은 맞는데 메시지가 한 개도 안 온다

**타입이나 QoS 가 다르면 연결 자체가 안 맺어집니다. 에러가 안 납니다.**
`ros2 topic list` 에도 정상으로 보이고 `echo` 로는 보이는데 노드만 못 받습니다.

브릿지·매니저가 3초마다 확인해서 이렇게 찍습니다.

```
[ERROR] camera 구동: '/camera_drive' 메시지 타입이 다르다 —
        /camera_node 는 std_msgs/msg/Int32 로 쏘는데
        우리는 std_msgs/msg/Float32 로 구독한다.

[ERROR] lidar 급정거: '/lidar_stop' QoS 가 안 맞는다 —
        /lidar_node 는 BEST_EFFORT, 우리는 RELIABLE.
```

**해결** — **발행하는 쪽**에서 타입/QoS 를 맞춥니다.

**직접 확인**

```bash
ros2 topic info -v /토픽이름
```

---

## D-3. 한 토픽에 발행자가 둘

값이 번갈아 들어와 튑니다.

```bash
ros2 topic info /odom        # Publisher count: 1 이어야 정상
ros2 topic info /mcu/ready
```

이번 배포에서 정리한 것:

| | 전 | 후 |
|---|---|---|
| 브릿지 odom | `/odom` + TF | `/mcu/odom`, TF 안 씀 |
| 매니저 ready | `/mcu/ready` | `/mcu/manager_ready` |

---

# E. 노드가 죽거나 이상할 때

## E-1. 매니저가 뜨자마자 죽는다 (`known_modes`)

```
InvalidParameterTypeException: known_modes ... BYTE_ARRAY
```

**원인** — `declare_parameter("known_modes", [])`. rclpy 는 기본값의 **원소**로
타입을 정하는데 빈 리스트에는 원소가 없어 `BYTE_ARRAY` 로 잡히고,
yaml 의 `STRING_ARRAY` 와 충돌합니다.

**해결** — 이번 배포에 고쳐져 있습니다(`Parameter.Type.STRING_ARRAY`).
**각자 고친 사본을 버리고 이 배포를 쓰세요.**

`params-file` 없이 `ros2 run` 으로 띄우면 목록이 비는데, 이제 죽지 않고
경고만 크게 찍습니다.

---

## E-2. 소스를 고쳤는데 옛날 대로 돈다

`ros2 run` 은 `src/` 가 아니라 **`install/` 안의 코드**를 실행합니다.

```bash
cd ~/T870_MCU && colcon build --symlink-install
```

`./실행.sh 점검` 이 `install/` 이 `src/` 보다 오래됐는지 봐줍니다.

---

## E-3. `ros2 run` 으로 브릿지·매니저를 따로 띄웠다

두 개가 뜨면 시리얼 포트를 서로 뺏고, `params-file` 을 빠뜨리기도 쉽습니다.

```bash
cd ~/T870_MCU && ./실행.sh
```

**이 하나로 둘 다 뜨고 설정도 자동으로 들어갑니다.**

---

# F. 차가 안 움직일 때

## F-1. 조종 창은 떴는데 WASD 가 안 먹는다

**`0` 키를 눌러 조종권을 잡아야 합니다.** 기본은 대기 상태입니다
(팀 노드를 방해하지 않으려고).

---

## F-1a. 🔴 mode_sim 을 눌러도 모드가 안 바뀐다

**0831 이전 `mode_sim.py` 는 동작하지 않습니다.** 두 가지가 낡아 있었습니다.

```
① 발행 토픽 기본값이 /vehicle_mode      매니저는 /drive_mode 를 구독
② 모드 목록에 S_COURSE 가 아예 없음     ACCELERATION/MANUAL 은 없는 모드
```

**해결** — `tools/mode_sim_v2_0831.py` 를 쓰세요. 옛 `mode_sim.py` 는 삭제했습니다.

```bash
cd ~/T870_MCU && source t870_env.sh
python3 tools/mode_sim_v2_0831.py
```

v2 는 **보낸 모드와 매니저가 받아들인 모드를 나란히** 보여줍니다.
다르면 화면이 빨갛게 알려주므로 토픽·이름 불일치를 바로 알 수 있습니다.

같은 부류(도구가 조용히 낡는 것)는 이제 `audit.py` 가 설치할 때 잡습니다.

---

## F-1b. 🔴 차는 가는데 조향이 안 된다 (S자 코스가 안 돌아감)

**조향 권한은 모드가 정합니다.** 지금 설정으로 `S_COURSE` 의 권한자는
**카메라**입니다. 라이다로 S자를 연습하면 매니저가 라이다 조향값을 무시하고
카메라를 기다리다가, 조향을 중앙에 두고 **직진만 합니다.**

**확인**

```bash
ros2 topic echo /mcu/active_wheel_source     # failsafe 가 나오면 확정
ros2 topic echo /mcu/safety_state            # WHEEL_FALLBACK(camera:never_received)
```

0831 버전부터는 매니저가 무엇을 고쳐야 하는지까지 로그로 찍습니다.

**해결** — `config/t870_mcu.yaml` 에서 주석을 풀고 다시 빌드합니다.

```yaml
wheel_owner_overrides:
  - "5:lidar"      # S 코스
  - "7:lidar"      # T 주차
  - "10:lidar"     # 평행 주차
```

> 0831 부터 모드는 **번호**입니다. `5` = S 코스, 전체 11구간.
> 표는 `모드_사용법.md` 에 있습니다.

```bash
cd ~/T870_MCU && colcon build --symlink-install
```

> yaml 은 `--symlink-install` 이어도 **다시 빌드해야 반영됩니다.**

자세한 내용은 **`모드_사용법.md`** 를 보세요.

---

## F-2. `safety=NO_DRIVE_SOURCE`

```
safety=NO_DRIVE_SOURCE(lidar:never_received, camera:never_received, gps:never_received)
```

아무도 구동 명령을 안 보내고 있다는 뜻입니다. 차는 안 움직이는 게 정상입니다.
수동으로 굴려보려면:

```bash
cd ~/T870_MCU && ./실행.sh 조종
```

---

## F-3. 후진이 안 된다 / `DIRECTION_CHANGE_PENDING` 만 계속 뜬다

```
DRIVE,DIRECTION_CHANGE_PENDING     ← 이것만 100ms 마다 무한 반복
DRIVE,DIRECTION_CHANGE_OK          ← 이게 안 뜸
```

**원인** — 펌웨어 v36 이하. 방향 전환은 완전 정지 후 300ms 를 기다려야 하는데,
상위가 10Hz 로 같은 명령을 보내면 대기 타이머가 100ms 마다 초기화돼
300ms 를 영원히 못 채웁니다.

**해결** — 펌웨어 **v37** 이 필요합니다. **MCU 담당(SJ)만 굽습니다.**
확인:

```bash
cd ~/T870_MCU && python3 tools/serial_console_v2_0830.py
```
부팅 배너에 `MCU_BOOT,v37` 이 나와야 합니다.

---

## F-4. 차가 0.5초마다 선다

팀 노드의 발행 주기가 부족합니다. **4Hz 이상**으로 계속 보내야 합니다.
펌웨어 워치독이 2초입니다.

---

## F-5. `ESTOP` 이 안 풀린다

조종 화면에서 `R`.

---

# G. 엔코더 · odom 값이 이상할 때

## G-1. `/mcu/encoder` 가 0, 1, 2 같은 낮은 값

**정상입니다.** 누적 카운트이고 부호가 있습니다(전진 +, 후진 −).
서 있으면 진동 때문에 ±1~2 흔들립니다.

그리고 `--once` 는 한 순간만 찍습니다. 세 번 따로 치면 2/0/1 이 나오는 게 당연합니다.

```bash
ros2 topic echo /mcu/encoder      # --once 를 빼고 띄워둔 채로 차를 민다
```

**손으로 1m 밀면 약 200 오릅니다.**

---

## G-2. 달리다 멈추면 엔코더가 0 으로 초기화된다

**원인** — 제동은 역토크 펄스라 전기 노이즈가 가장 큰 순간입니다.
그때 STATUS 줄이 깨지면 예전 코드는 그 필드를 **0 으로 읽고 발행**했고,
누적 카운터에 0 이 튀니 "초기화" 로 보였습니다.

**해결** — 이번 배포에 고쳐져 있습니다. 못 읽으면 발행하지 않고 직전 값을
유지합니다. 로그에 이게 뜨면 엔코더 배선을 모터선과 떼어 놓으세요.

```
[WARN] 엔코더가 6400 → 0 으로 0.05초 만에 튀었다 — 이 STATUS 는 버린다
```

---

## G-3. 🔴 odom 거리가 실제와 몇 배씩 다르다

**이건 고장이 아닙니다. 구동 바퀴가 헛돕니다.**

0830 실측 (1단 15초, 같은 명령 3회):

| 실제 이동 | 카운트 | 슬립 |
|---|---|---|
| 1.73 m | 2052 | 83% |
| 4.98 m | 2543 | 61% |
| 6.32 m | 2917 | 57% |

같은 명령인데 **간 거리가 3.6배** 달랐습니다.

확인한 것:
- 엔코더 눈금은 정확 (1단 회귀 기울기 181.7 ≈ 스펙 199.5 counts/m)
- 전기 노이즈 아님 (바퀴를 고정하고 구동 → 2초에 23카운트뿐)
- **원인은 구동륜 슬립 하나**

**그래서 `counts_per_meter` 를 아무리 맞춰도 안 됩니다.**

| 토픽 | 신뢰도 |
|---|---|
| `/mcu/encoder` | ✅ 바퀴 회전량으로는 정확 |
| `/mcu/distance_m` | ⚠ "바퀴가 굴러간 거리". 지면 거리 아님 |
| `wheel_odom` → `/odom` | 🔴 **위치 추정에 쓰지 마세요** |

**위치는 GPS/IMU 로 잡아주세요.**
근본 해결은 **구동하지 않는 앞바퀴에 엔코더**를 다는 것입니다.
펌웨어 자리(D18/D19)는 이미 준비돼 있습니다.

---

## G-4. 엔코더가 거의 안 오른다 (2~3m 에 몇 카운트)

**원인** — 펌웨어의 `ENCODER_DEBOUNCE_US` 가 200 이 아닙니다.

```bash
cd ~/T870_MCU && python3 tools/serial_console_v2_0830.py
```
부팅 배너 확인:
```
ENCODER,CPR,163.0,DEBOUNCE_US,200,UPDATE_MS,100
```

**`DEBOUNCE_US` 는 반드시 200 이어야 합니다.** 2000 이면 카운트를 99% 잃습니다.
`counts_per_meter 199.8` 도 200us 기준으로 측정한 값이라 **이 둘은 한 쌍입니다.**

다르면 SJ 에게 알려주세요. 펌웨어는 MCU 담당만 굽습니다.

---

# H. 조향

## H-1. 켜자마자 바퀴가 혼자 움직인다

**0830 부터 없앴습니다.**

```yaml
center_steer_on_connect: false
```

아직 움직인다면 옛 배포입니다. v12 이상으로 받으세요.

---

## H-2. 직진이 한쪽으로 휜다 / 중앙을 모르겠다

**조향각 센서가 아직 없습니다.** A0 값은 참고용이라 판정에 쓰면 안 됩니다.
차가 실제로 곧게 가는지만 보고 잡습니다.

```bash
pkill -f "mcu_bridge|mcu_manager"; sleep 1
cd ~/T870_MCU && python3 tools/center_v2_0830.py
```

1. `t` 로 5초 달려본다
2. 왼쪽으로 휘면 `d`, 오른쪽으로 휘면 `a` (처음엔 `A`/`D` 로 10ms 씩)
3. 곧게 갈 때까지 반복, 가까워지면 `a`/`d` 로 1ms 씩
4. 곧게 가면 **`m`** → 그 자리가 중앙(누적 0)으로 등록

> ⚠ **등록한 중앙은 아두이노 전원을 끄면 사라집니다.**
> 펌웨어가 부팅할 때 조향 누적을 0 으로 잡기 때문입니다.
> 전원을 새로 넣을 때마다 다시 잡아주세요.

---

## H-3. 좌우 회전 각도가 다르다

개루프(시간 기반) 조향이라 오차가 누적됩니다. 각도 센서(AS5600) 도입 전까지는
`center_v2_0830.py` 로 그때그때 잡는 것이 최선입니다.

---

# I. 그 밖에

## I-1. 주행이 초반에 뚝뚝 끊긴다

**원인** — 도구가 시리얼을 과하게 씁니다. 아두이노 수신 버퍼가 64바이트라
넘치면 구동 명령이 씹힙니다.

**해결** — `run_v4_0830.py` 이상을 쓰세요. 펌웨어 자동 발행을 켜서
주행 중에는 아무것도 묻지 않습니다.

---

## I-2. 아두이노가 주행 중 끊긴다

USB 케이블 접촉 불량이 대부분입니다. 로그를 그대로 남겨 주세요.

---

## I-3. 되돌리고 싶다

`설치.sh` 는 아무것도 지우지 않습니다. 전부 여기 있습니다.

```bash
ls ~/t870_backup_*/
```
