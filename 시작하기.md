# T870 MCU — 팀원용 설치 안내 (2026-08-30 v8)

> **이 zip 하나만 받으면 됩니다.** 다른 파일 필요 없습니다.
> 각자 고쳐 쓰던 `manager_node.py` 사본이 세 갈래로 갈라져 있었습니다.
> 이 버전이 그걸 하나로 합친 것입니다. **각자 고친 것은 버리고 이걸 쓰세요.**

이 폴더 하나면 끝난다. 옛날 파일은 스크립트가 알아서 백업으로 치운다.

---

## 0. 먼저 — 지금 돌고 있는 노드 전부 끄기

브릿지가 시리얼 포트를 잡고 있으면 새로 못 뜬다.
켜둔 터미널마다 `Ctrl-C`.

---

## 1. 설치

```bash
cd ~/Downloads
unzip -o T870_MCU_0830_팀배포_v13.zip
cd T870_MCU_0830

./설치.sh                 # ① 먼저 이걸로 뭘 할지 본다 (아무것도 안 바꿈)
./설치.sh --apply         # ② 확인했으면 이걸로 실제 설치
```

워크스페이스가 `~/T870_MCU` 가 아니면 경로를 붙인다:

```bash
./설치.sh --apply ~/내워크스페이스
```

### 스크립트가 하는 일

1. 돌고 있는 노드가 있는지 본다 (있으면 멈춘다)
2. 홈 아래 `t870_mcu` 패키지가 **여러 군데** 있는지 찾아서 알려준다
3. 옛날 `src/t870_mcu`, `build/`, `install/`, `log/`, `tools/` 를
   `~/t870_backup_<날짜>/` 로 **옮긴다** (지우지 않는다)
4. 새 파일을 넣고 `colcon build`

> **`build/` `install/` 를 왜 치우나**
> `ros2 run` 은 `src/` 가 아니라 `install/` 안의 코드를 실행한다.
> 여기에 옛날 게 남아 있으면 소스를 바꿔도 **옛날 코드가 계속 돈다.**
> 지금 겪는 이상한 증상 대부분이 여기서 온다.

---

## 1-b. ★ 설치 후 자기 PC 점검 (권장)

"나는 되는데 너는 안 된다" 의 원인을 미리 찾아준다.

```bash
cd ~/T870_MCU
./실행.sh 점검
```

무엇을 보나:

| 항목 | 왜 |
|---|---|
| `ROS_DOMAIN_ID` / RMW | 팀끼리 다르면 **노드가 서로 아예 안 보인다** |
| `dialout` 그룹 | 없으면 포트 열 때 permission denied |
| 시리얼 포트를 누가 잡고 있나 | 브릿지가 두 개 뜨면 서로 뺏는다 |
| **`install/` 이 `src/` 보다 오래됐나** | ★ 소스를 고쳐도 옛날 코드가 도는 원인 |
| `t870_mcu` 가 여러 군데 깔렸나 | 어느 것이 도는지 모르게 된다 |
| 한 토픽에 발행자가 둘 이상인가 | 값이 번갈아 들어와 튄다 |

노드를 띄운 뒤 다시 돌리면 토픽 충돌까지 같이 본다.

소스와 yaml 의 아귀는 설치할 때 자동으로 검사한다(`tools/audit.py`).
직접 돌려도 된다:

```bash
python3 tools/audit.py ~/T870_MCU
```

---

## 1-c. ★★ 0830 에 바뀐 것 (이번 배포의 핵심)

### ① 시작할 때 바퀴가 혼자 중앙으로 가던 것 — 없앴습니다

브릿지가 연결되면 조향을 자동으로 중앙 정렬하고 있었습니다.
차를 어디에 세워 두든 켜자마자 바퀴가 제멋대로 움직였고,
사람이 잡고 있으면 위험합니다.

```yaml
center_steer_on_connect: false     # ★ 0830
```

**이제 켜도 바퀴는 그대로 있습니다.** 정렬이 필요하면 직접:

```bash
./실행.sh 조종        # 창에서 0 → A / D 로 맞춤
```

### ② "달리다 멈추면 엔코더가 0 으로 초기화" — 고쳤습니다

원인은 엔코더가 아니라 **시리얼 파싱**이었습니다.
제동은 역토크 펄스라 전기 노이즈가 가장 큰 순간인데, 그때 STATUS 줄이
깨지면 예전 코드는 그 필드를 **0 으로 읽고 그대로 발행**했습니다.
누적 카운터에 0 이 튀니 구독자 쪽에서는 "멈추면 초기화" 로 보였습니다.

이제 못 읽으면 **발행하지 않고 직전 값을 유지**합니다.
물리적으로 불가능한 점프도 같이 걸러냅니다.

```yaml
encoder_max_counts_per_s: 2000.0   # 이보다 빨리 변하면 깨진 값으로 본다
```

로그에 이게 뜨면 엔코더 배선을 모터선과 떼어 놓아야 합니다:

```
[WARN] 엔코더가 6400 → 0 으로 0.05초 만에 튀었다 — 이 STATUS 는 버린다
```

### ③ odom 이 실측과 다를 때 — 누구 문제인지 갈라주는 도구

```bash
python3 tools/odom_compare.py
```

줄자 / `/mcu/encoder` / `/mcu/distance_m` / `/odom` **네 값을 한 번에** 재서
어느 단계에서 어긋나는지 표로 보여줍니다.
"우리 값은 맞고 그쪽만 틀리다" 까지 찍어줍니다. 서로 떠넘기지 않으려는 도구입니다.

### ④ 펌웨어 v37 (SJ 가 굽습니다. 팀원은 굽지 마세요)

- **v36** — 엔코더 디바운스를 200us 로 되돌림.
  v34 에서 2000us 로 올렸다가 카운트를 99% 잃었습니다.
  `counts_per_meter 199.8` 은 **200us 기준**이라 이 둘은 한 쌍입니다.
  부팅할 때 실제 설정값을 찍으니 이제 추측하지 않아도 됩니다.
- **v37** — 방향 전환 교착 수정.
  `DRIVE,DIRECTION_CHANGE_PENDING` 만 계속 뜨고 차가 안 바뀌던 것.
  10Hz 로 명령이 들어오면 300ms 대기가 100ms 마다 다시 시작돼
  영원히 안 바뀌었습니다.
- 경사로 자동 안티롤백(`AR1`/`AR2`/`AR0`)도 들어가 있습니다.

---

### ⑤ `설치.sh` 가 빌드에서 죽던 것 — 고쳤습니다 (v12)

```
/opt/ros/jazzy/setup.bash: line 8: AMENT_TRACE_SETUP_FILES: unbound variable
✗ 빌드 실패
```

이게 떴다면 **v11 을 받으신 겁니다. v12 로 다시 받으세요.**
스크립트 잘못이지 PC 문제가 아닙니다.

ROS 의 `setup.bash` 는 정의되지 않은 변수를 참조하는데, 스크립트가
`set -u`(정의 안 된 변수를 쓰면 즉시 종료)를 켠 채로 그걸 불러서
그 줄에서 죽었습니다. 소싱하는 동안만 끄도록 고쳤습니다.

git 으로 받는 분을 위해, **클론 안에서 `설치.sh` 를 돌리면 멈추고 안내**하도록
했습니다. 클론 자체가 워크스페이스라 `colcon build` 만 하면 됩니다.

---

## 1-d. ★ 이전 배포에서 바뀐 것 (각자 고친 사본을 대체합니다)

### `known_modes` 선언 방식 — 세 갈래를 하나로

`declare_parameter("known_modes", [])` 가 매니저를 죽이던 문제입니다.
rclpy 는 기본값의 **원소**로 타입을 정하는데, 빈 리스트에는 원소가 없어
`BYTE_ARRAY` 로 잡히고 yaml 의 `STRING_ARRAY` 와 충돌해 죽습니다.

세 팀이 각자 다르게 고쳐 사본이 갈라져 있었습니다:

| 누가 | 어떻게 | 문제 |
|---|---|---|
| 카메라팀 사본 | 모드 목록을 코드에 박음 | 이름 바뀌면 yaml 과 어긋남 |
| 라이다팀 | `Parameter.Type.STRING_ARRAY` | ✅ 방식은 이게 맞음 |
| 이전 배포 | `[""]` 자리표시자 | 동작은 하나 설명이 필요 |

**라이다팀 방식으로 통일했습니다.** Jazzy rclpy 에서 직접 확인해 준 방식이고
`ros2 param describe` 결과도 `string array` 로 나옵니다. 알려주셔서 감사합니다.

목록은 **yaml 한 곳에만** 둡니다. 코드에 박지 마세요 —
구간 이름이 바뀔 때 yaml 만 고친 사람과 어긋나고, 그게 조용히 일어납니다.

`params-file` 없이 `ros2 run` 으로 띄우면 목록이 비는데, 이제 죽지 않고
**경고를 크게 찍고** 검증만 꺼집니다.

### 구간 모드 이름 — GPS팀 이름으로 통일

GPS팀 경로 CSV 의 `mode` 열 값을 그대로 씁니다.

| 예전 (우리) | 지금 (GPS팀) |
|---|---|
| `INTERSECTION_1/2/3` | **`INTERSECTION`** |
| `CRANK` | **`D_COURSE`** |
| `ACCELERATION` | **`ACCEL`** |
| `START` `FINISH` `SUDDEN_STOP` | 없앰 (GPS CSV 에 없음) |
| `SLOPE` `S_COURSE` `T_PARK` `PARALLEL_PARK` | 그대로 |

전체 목록: `IDLE NORMAL SLOPE D_COURSE INTERSECTION S_COURSE T_PARK ACCEL PARALLEL_PARK OUT`

### 구간 모드 토픽 — `/vehicle_mode` → `/drive_mode`

GPS팀 `gps_route_follower` 가 **`/vehicle_mode` 를 Int32(1~11)** 로 이미
쓰고 있습니다. 우리는 String 을 기다리므로 이름이 같아도 **한 개도 안 들어옵니다.**
에러도 안 납니다.

문자열 구간 이름은 그쪽이 **`/drive_mode` (String)** 로 따로 냅니다.
그래서 우리 `mode_topic` 을 그쪽으로 옮겼습니다.

```bash
ros2 topic echo /drive_mode        # T_PARK 같은 문자열이 나와야 정상
ros2 topic echo /mcu/current_mode  # 매니저가 받아들인 모드
```

### 백업 폴더에 `COLCON_IGNORE`

`colcon build` 가 백업 안의 옛 `t870_mcu` 를 같이 잡아 **패키지 이름 중복으로
빌드가 통째로 실패**하던 문제입니다. (라이다팀이 실제로 막혔습니다)

이제 `설치.sh` 가 백업 폴더에 `COLCON_IGNORE` 를 만들어 둡니다.
워크스페이스 안에 남아 있는 다른 사본도 찾아서 같이 처리합니다.

> 이미 그 상태로 막혀 있다면 수동으로도 됩니다:
> ```bash
> touch ~/t870_backup_*/COLCON_IGNORE
> ```

---

## 2. 실행 — 이제 이 세 개가 전부다

설치가 끝나면 워크스페이스에 `실행.sh` 가 생긴다.
**`source` 도 `export` 도 이 스크립트가 알아서 한다.**
터미널을 새로 열 때마다 뭘 칠 필요가 없다.

```bash
cd ~/T870_MCU

./실행.sh 점검      # 이 PC 환경 점검 — 안 될 때 제일 먼저
./실행.sh           # 브릿지 + 매니저 실행 (평소 이것)
./실행.sh 조종      # 터미널로 직접 운전 (WASD)
./실행.sh 상태      # 지금 뭐가 도는지 한눈에
```

`./실행.sh 조종` 은 창이 뜨면 **`0` 키를 눌러야 조종권을 잡는다.**
그 전에는 팀 노드를 방해하지 않으려고 아무것도 발행하지 않는다.
(`W A S D` 주행 / `F` 급정거 / `E` 비상정지 / `Q` 종료)

> **`ros2 run` 으로 브릿지·매니저를 따로 띄우지 말 것.**
> 두 번 뜨면 시리얼 포트를 서로 뺏고, params-file 을 빠뜨리기도 쉽다.
> `./실행.sh` 하나면 둘 다 뜨고 설정도 자동으로 들어간다.

포트는 자동으로 찾는다. 하드코딩된 `/dev/ttyACM0` 는 없앴다.

### DOMAIN_ID 와 RMW 는 `t870_env.sh` 한 곳에만 있다

```bash
export ROS_DOMAIN_ID=77
export RMW_IMPLEMENTATION=rmw_fastrtps_cpp
```

**ROS 2 는 이 둘 중 하나만 달라도 노드끼리 서로 존재조차 모른다.**
에러도 안 난다. "나는 되는데 너는 안 되는" 상황의 1순위 원인이다.

값은 2026-08-29 기준 팀이 실제로 쓰던 것(77 / Fast-DDS)을 넣어 뒀다.
**각자 고치지 말 것.** 바꿔야 하면 MCU 담당에게 말하고 전원이 같이 받는다.

다른 워크스페이스(`mmission_ws` 등)를 띄우는 터미널에서도 같은 값을 써야 한다:

```bash
source ~/T870_MCU/t870_env.sh     # 그 터미널에서도 이걸 먼저
```

---

## 3. ★ wheel_odom 실행 명령이 바뀐다

지금 쓰던 명령:

```bash
-p encoder_m_per_tick:=0.00187582      # ✗ 옛날 값
```

`1 ÷ 0.00187582 = 533.1 counts/m` 인데, **실측은 199.8** 이다.
이 값이면 odom 이 **실제 거리의 37.5% 만** 보고한다. (10m 가면 3.75m 라고 함)

바꿀 값은 `1 ÷ 199.8 = 0.005005`:

```bash
ros2 run mission_manager wheel_odom --ros-args \
  -p encoder_topic:=/mcu/encoder \
  -p yaw_topic:=/imu/relative_yaw_deg \
  -p odom_topic:=/odom \
  -p encoder_m_per_tick:=0.005005
```

---

## 4. ★ /odom 과 TF 주인을 wheel_odom 으로 넘겼다

전에는 이랬다:

| | MCU 브릿지 | wheel_odom |
|---|---|---|
| 토픽 | `/odom` | `/odom` |
| TF | `odom → base_link` | `odom → base_footprint` |

**둘 다 쏘고 있었다.** 퍼블리셔가 둘이면 메시지가 번갈아 들어와 위치가 튀고,
TF 는 `odom` 밑에 자식이 둘이 생겨 트리가 깨진다.

이번 yaml 에서 브릿지 쪽을 뺐다:

```yaml
pub_topic_odom: "/mcu/odom"   # /odom → 참고용으로 이동
publish_tf: false             # TF 는 wheel_odom 이 담당
```

wheel_odom 이 IMU yaw 를 쓰니까 그쪽이 맞다.
(우리 브릿지는 조향각으로 yaw 를 추정하는데, 그 조향각을 아직 못 믿는다)

### 확인

```bash
ros2 topic info /odom
```

**`Publisher count: 1`** 이어야 한다. 2 면 아직 겹치는 것이다.

### ⚠ 이거 빠지면 Nav2 가 깨진다

이제 아무도 `base_link` 를 TF 트리에 안 올린다.
`base_footprint → base_link` 를 누가 쏘는지 확인할 것.
URDF 에 있으면 그걸로 되고, 없으면 임시로:

```bash
ros2 run tf2_ros static_transform_publisher 0 0 0 0 0 0 base_footprint base_link
```

---

## 4-b. ★ `/mcu/ready` 이름이 바뀌었다

브릿지와 매니저가 **같은 `/mcu/ready` 에 둘 다 쏘고 있었다.**
타입도 Bool 로 같아서 에러가 안 났다. 뜻만 달랐다.

| | 전 | 후 |
|---|---|---|
| 브릿지 (아두이노 통신 살아있나) | `/mcu/ready` | `/mcu/ready` (그대로) |
| 매니저 (중재기가 명령 내보내나) | `/mcu/ready` | **`/mcu/manager_ready`** |

`/mcu/ready` 를 구독하고 있었다면 어느 쪽을 원한 건지 확인할 것.

---

## 4-c. 메시지가 안 오면 로그를 볼 것

토픽 이름이 같아도 **메시지 타입이나 QoS 가 다르면 한 개도 안 들어온다.**
그런데 ROS 는 에러를 내지 않는다. `ros2 topic list` 에도 정상으로 보이고,
`ros2 topic echo` 로는 보이는데 노드만 못 받는다.

이번부터 브릿지와 매니저가 3초마다 확인해서 이렇게 찍는다:

```
[ERROR] camera 구동: '/camera_drive' 메시지 타입이 다르다 —
        /camera_node 는 std_msgs/msg/Int32 로 쏘는데
        우리는 std_msgs/msg/Float32 로 구독한다. 이러면 한 개도 안 들어온다.

[ERROR] lidar 급정거: '/lidar_stop' QoS 가 안 맞는다 —
        /lidar_node 는 BEST_EFFORT 로 쏘는데 우리는 RELIABLE 로 구독한다.
        연결 자체가 안 맺어져 한 개도 안 들어온다.
```

이게 뜨면 **발행하는 쪽에서** 타입이나 QoS 를 맞춰야 한다.
(발행자가 아직 없는 건 정상이라 조용히 넘어간다)

---

## 5. `/mcu/encoder` 값이 0, 1, 2 로 낮게 나오는 것 — 정상이다

이건 **누적 카운트이고 부호가 있다** (전진 +, 후진 −).
차가 서 있으면 0 근처에서 진동 때문에 ±1~2 왔다갔다 한다. 고장이 아니다.

그리고 `--once` 는 한 순간만 찍는다. 세 번 따로 치면 2 / 0 / 1 이 나오는 게 당연하다.

제대로 보려면 `--once` 를 빼고 띄워놓은 채로 차를 밀어볼 것:

```bash
ros2 topic echo /mcu/encoder
```

숫자가 쭉 오르면 엔코더는 멀쩡하다. **1m 밀면 약 200 오른다.**

### wheel_odom 쪽에서 확인해야 할 것

우리가 보내는 `/mcu/encoder` 는 **누적값**이고 **이미 부호가 붙어 있다.**

- wheel_odom 이 이걸 델타(직전 대비 증분)로 읽고 있으면 → 거리가 폭주한다
- `abs()` 를 한 번 더 씌우고 있으면 → 후진이 전진으로 잡힌다

둘 중 하나면 알려줄 것. 브릿지 쪽에서 맞춰줄 수 있다.

---

## 6. 구동 소스가 안 들어올 때

매니저 로그에 이게 뜨면:

```
safety=NO_DRIVE_SOURCE(lidar:never_received, camera:never_received, gps:never_received)
```

**아무도 구동 명령을 안 보내고 있다는 뜻이다.** 차는 안 움직인다.
이 상태에서 엔코더가 0 인 건 당연하다.

수동으로 굴려보려면:

```bash
cd ~/T870_MCU
python3 tools/drive_wasd.py --takeover
```

- `0` 조종권 잡기 / 놓기 (기본은 대기 상태라 팀 노드를 방해하지 않는다)
- `W A S D` 주행 / `F` 급정거 / `E` 비상정지 / `Q` 종료

---

## 7. ROS_DOMAIN_ID 는 `~/.bashrc` 에 적지 말 것

`t870_env.sh` 한 곳에만 둔다 (`77` / Fast-DDS).
`~/.bashrc` 에도 적어 두면 나중에 값을 바꿀 때 그것만 남아
**본인만 다른 도메인에 혼자 떠 있게 된다.** 그러면 아무것도 안 보인다.

이미 적어 뒀다면 지울 것:

```bash
grep -n 'ROS_DOMAIN_ID\|RMW_IMPLEMENTATION' ~/.bashrc
```

나오면 그 줄을 지우고 `source ~/.bashrc`.

---

## 8. 펌웨어는 건드리지 말 것

`참고/T870_MCU_v37.ino` 는 **읽어보라고 넣은 것이다. 굽지 말 것.**
차량 펌웨어 업로드는 MCU 담당(SJ)만 한다.
서로 다른 버전이 올라가면 어느 코드가 도는지 아무도 모르게 된다.

---

## 문제 생기면

터미널에 뜬 메시지를 **자르지 말고 그대로** SJ 한테 보낼 것.
`~/t870_backup_<날짜>/` 에 옛날 파일이 전부 남아 있으니 되돌릴 수 있다.
