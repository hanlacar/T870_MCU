# T870 MCU — 시작하기

## 0. 먼저 — 돌고 있는 노드 전부 끄기

브릿지가 시리얼 포트를 잡고 있으면 새로 못 뜹니다. 켜둔 터미널마다 `Ctrl-C`.

---

## 1. 설치 — 한 번만

### (A) 처음이거나, 다 지우고 새로 까는 경우

```bash
cd ~ && git clone https://github.com/hanlacar/T870_MCU mcu_ws
```
```bash
cd ~/mcu_ws && colcon build --symlink-install
```
```bash
cd ~/mcu_ws && ./setup.sh
```

`setup.sh` 는 아두이노 포트 고정 이름(`/dev/t870_mcu`)을 만듭니다. 한 번만 하면 됩니다.

### (B) 이미 쓰던 폴더가 있는 경우 — `설치.sh` 를 쓰세요

옛날 파일이 남아 있으면 **소스를 바꿔도 옛날 코드가 계속 돕니다.**
`ros2 run` 은 `src/` 가 아니라 `install/` 안의 코드를 실행하기 때문입니다.
이상한 증상 대부분이 여기서 옵니다. `설치.sh` 가 그걸 정리해 줍니다.

먼저 **뭘 할지 보기만** 합니다. 아무것도 안 바꿉니다:
```bash
cd ~/mcu_ws && ./설치.sh
```

확인했으면 **실제로 실행**:
```bash
cd ~/mcu_ws && ./설치.sh --apply
```

워크스페이스 경로가 다르면 뒤에 붙입니다:
```bash
./설치.sh --apply ~/내워크스페이스
```

**설치.sh 가 하는 일**
1. 돌고 있는 노드가 있는지 확인 (있으면 멈춤)
2. 홈 아래 `t870_mcu` 패키지가 여러 군데 있는지 찾아서 알려줌
3. 옛날 `src/t870_mcu`, `build/`, `install/`, `log/`, `tools/` 를
   `~/t870_backup_<날짜>/` 로 **옮김** (지우지 않음 — 되돌릴 수 있게)
4. 새 파일을 넣고 `colcon build`

### 설치 확인

```bash
cd ~/mcu_ws && ./실행.sh 점검
```
경고가 하나도 없어야 정상입니다. **"나는 되는데 너는 안 된다" 의 원인을 미리 찾아줍니다.**

```bash
cd ~/mcu_ws/src/t870_mcu && PYTHONPATH=. python3 -m pytest test/ -q
```
`128 passed` 가 나와야 합니다.

---

## 2. 업데이트 — 그 뒤로는 이것만

```bash
cd ~/mcu_ws && git pull && colcon build --symlink-install
```

빌드가 이상하면 캐시를 지우고 다시:
```bash
cd ~/mcu_ws && rm -rf build install log && colcon build --symlink-install
```

---

## 3. 실행 — **창 하나에 하나씩**

| 창 | 명령 | 하는 일 |
|---|---|---|
| 1 | `./실행.sh 브릿지` | 아두이노 연결 (시리얼) |
| 2 | `./실행.sh 중재` | 명령 중재기 (모드·우선순위) |
| 3 | `./실행.sh 조종` | WASD 수동 운전 |
| 4 | `./실행.sh 프레임` | 센서 위치 TF |

창을 아끼고 싶으면 1+2 를 한 창에:
```bash
cd ~/mcu_ws && ./실행.sh 전체
```

> **조종은 반드시 별도 창.** 브릿지가 다른 창에 떠 있어야 합니다.
> 조종 창이 뜨면 **0 키**를 눌러야 조종권을 잡습니다.

인자를 빼면 목록이 나옵니다:
```bash
cd ~/mcu_ws && ./실행.sh
```

---

## 4. 안 될 때

```bash
cd ~/mcu_ws && ./실행.sh 점검
```
도메인·포트·빌드·중복을 한 번에 봅니다. **제일 먼저 이것.**

```bash
cd ~/mcu_ws && ./실행.sh 상태
```
지금 무엇이 돌고 있는지, 토픽 발행자가 몇인지.

### 자주 나오는 것

| 증상 | 원인 | 조치 |
|---|---|---|
| 상대 노드가 안 보임 | 도메인 불일치 | 모든 창에서 `source ~/mcu_ws/t870_env.sh` (42로 맞춰집니다) |
| `아두이노 끊김` | 브릿지가 안 떠 있음 | 창1에서 `./실행.sh 브릿지` |
| `조향이 중앙으로 고정` | 권한자 노드가 안 떠 있음 | 그 팀 노드를 띄우세요. 안 띄웠으면 **정상** |
| 노드 이름 중복 | 같은 걸 두 번 띄움 | 하나를 Ctrl-C |
| `/dev/ttyACM0` 를 bridge 가 사용 중 | **정상** | 우리 브릿지입니다. 끄지 마세요 |
| 빌드 실패 | 캐시 | `rm -rf build install log` 후 재빌드 |
| 실차인데 이상 | 시뮬 켜짐 | `ros2 node list | grep sim` → `/mission_sim` 끄기 |

---

## 5. 다른 팀이 보내야 하는 것

| 팀 | 토픽 | 타입 |
|---|---|---|
| 카메라 | `/camera_drive` `/camera_wheel` `/camera_stop` | Float32 / Int32 / Bool |
| 라이다 | `/lidar_drive` `/lidar_wheel` `/lidar_stop` `/avoidance/active` | Float32 / Int32 / Bool / Bool |
| GPS | `/drive_mode` `/gps_drive` `/gps_wheel` | String("0"~"11") / Float32 / Int32 |

구동값은 `-1, 0, 1, 2, 3` 만 됩니다. 조향은 `-27 ~ +27` 도, **+가 우측**입니다.

자세한 점검 명령은 `팀별_MCU_연동_점검표_0901.md` 를 보세요.

---

## 6. 꼭 알아둘 것

**MCU 가 `/odom` 과 `odom→base_link` TF 를 발행합니다.**
미션팀 `wheel_odom_node`, 주차팀 `mcu_odom_adapter.py` 는 **꺼야 합니다.**
확인: `ros2 topic info /odom` → `Publisher count: 1`

**축거는 0.73 m** 입니다 (0901 실측·검산). 주차팀 코드의 0.77 은 고쳐야 합니다.

**오돔은 아직 못 믿습니다.** 4WD 오픈 디퍼렌셜이라 바퀴가 헛돕니다.
같은 명령 15초에 간 거리가 1.73 / 4.98 / 6.32 m 로 달랐습니다.
위치추정의 주 소스로 쓰지 마세요.
