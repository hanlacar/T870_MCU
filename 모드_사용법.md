# 구간 모드 — 번호 방식 (0831부터)

## 바뀐 것 한 줄

**모드 이름을 버리고 경로 순번(0~12)을 씁니다.**

```
전:  /drive_mode  →  "S_COURSE"
후:  /drive_mode  →  "5"
```

## 왜 바꿨나

카메라는 구간마다 쓰는 로직이 전부 다릅니다. 그런데 이름 방식에서는
**`NORMAL` 이 경로에 5번 나옵니다.** 카메라가 받는 값이 그냥 `"NORMAL"` 이라
**몇 번째 NORMAL 인지 알 수가 없었습니다.**

번호는 그 자체가 구간 식별자라 이 문제가 사라집니다.

---

## 번호표 (0831 확정 — GPS팀 구간 정의 기준)

| 번호 | 구간 | 조향 권한 |
|---|---|---|
| 0 | 대기 (출발 전) | camera |
| 1 | 출발 ~ 경사로 전 | camera |
| 2 | 경사로 | camera |
| 3 | 직각코너 ~ 정지선 | camera |
| 4 | 정지선 ~ 교차로 ~ 횡단보도 | camera |
| **5** | **S자** | camera → **lidar** ★ (게이트) |
| 6 | 정지선 ~ T주차 우회전 | camera |
| **7** | **T주차** | **lidar** ★ |
| 8 | T주차 ~ 정지선 | camera |
| 9 | 가속 후 좌회전 | camera |
| **10** | **좌회전 후 평행주차** | **lidar** ★ |
| 11 | 출차 — 종점까지 주행 | camera |

**10번은 주차만이 아니라 도로 복귀까지 책임집니다.** 차가 다시 도로로 나온
뒤에 11번으로 넘어갑니다. 11번은 종점까지 가는 일반 주행이라 카메라가 잡고,
GPS 상태가 좋으면 GPS 를 씁니다.

**5번은 다른 구간과 규칙이 다릅니다.** 평소에는 카메라가 조향하고,
`/avoidance/active` 가 true 인 동안만 라이다가 가져갑니다. 신호가 꺼지면
바로 카메라로 돌아옵니다.

## 조향 폴백 (0902 추가)

권한자가 값을 안 보내면 **다음 순서로 내려갑니다.**

```
camera → lidar → gps → (아무도 없으면 중앙 고정)
```

0831 까지는 폴백이 없어서, 권한자가 침묵하면 곧바로 중앙 고정(직진)이었습니다.
구동은 `lidar → camera → gps` 로 내려가는데 조향만 안 내려갔습니다.
그래서 **카메라 노드를 안 띄우고 GPS 만 꽂아 시험하면 차가 직진만** 했습니다.

지금은 GPS 만 있어도 GPS 가 조향을 이어받습니다. 원래 권한자가 값을 다시
보내면 **자동으로 되돌아갑니다.** 폴백 중에는 로그에 이렇게 뜹니다.

```
조향 폴백 — 모드 '1' 의 권한자가 값을 안 줘서 'gps' 가 조향을 이어받았다.
```

바꾸려면 `config/t870_mcu.yaml` 의 `wheel_fallback_chain` 을 고칩니다.
비워두면(`[]`) 예전 동작(즉시 중앙 고정)으로 돌아갑니다.

---

> 0830 까지는 `"5:lidar"` 로 고정해 뒀는데, 그러면 회피 신호가 없는
> 평상시에 라이다가 게이트에 막혀 **조향이 중앙(직진)으로 폴백** 됐습니다.
> S자 한복판에서 직진한다는 뜻입니다. 0831 부터 게이트가 열린 동안에만
> 소유자를 넘기는 방식으로 바꿨습니다
> (`avoidance_gate_wheel_handover: true`).

바꾸려면 `config/t870_mcu.yaml` 의 `wheel_owner_overrides` 만 고치면 됩니다.

```yaml
wheel_owner_overrides:
  - "7:lidar"      # T 주차
  - "10:lidar"     # 평행 주차 (주차 + 도로 복귀)
```

> **조향 권한이 없는 소스가 보낸 조향값은 무시됩니다.**
> 이게 0830 에 라이다로 S자를 연습했는데 차가 직진만 하던 이유입니다.

---

## GPS팀은 아직 안 바꿔도 됩니다

`mode_aliases` 로 **옛 이름이 와도 번호로 받습니다.**

```yaml
mode_aliases:
  - "S_COURSE:5"
  - "T_PARK:7"
  - "PARALLEL_PARK:10"
  ...
```

로그에 이렇게 뜹니다.

```
[INFO] 모드 별칭: 'S_COURSE' → '5' 로 받는다.
       발행하는 쪽이 번호로 바꾸면 yaml 의 mode_aliases 에서 지울 것.
```

> ⚠ **이름으로는 구분이 안 되는 것이 있습니다.**
> · CSV 의 `NORMAL` 은 1·4·6·8·11 중 어느 것인지 알 수 없어 전부 `1` 로 들어옵니다.
> · CSV 의 `INTERSECTION` 은 위치상 좌회전 자리라 `8` 로 들어옵니다.
>
> **즉 이름으로 받는 동안에는 4·6번 교차로를 구분할 수 없습니다.**
> 구분이 이번 변경의 목적이므로 **GPS팀이 번호를 발행해야 완성됩니다.**

---

# 실행법

## 1. 키보드로 (mode_sim)

```bash
cd ~/T870_MCU && ./실행.sh
```

새 터미널:

```bash
cd ~/T870_MCU && source t870_env.sh
python3 tools/mode_sim_v2_0831.py
```

키:

```
0~9, a, b      모드 전환 (a=10  b=11)
c/l/g          카메라/라이다/GPS 발행 ON·OFF
C/L/G          그 소스의 drive 단계 순환
← →            선택된 소스의 조향각 ±5도
t              조향 편집 대상 전환
s              lidar_stop 토글    e  estop_lock 토글
q              종료
```

**S자 코스 연습이면 `5` 를 누르고, `l` 로 라이다를 켭니다.**

화면에 이렇게 나와야 정상입니다:

```
│  보낸 모드   5      S 코스  곡선 + 장애물     ★ 라이다 조향
│  매니저 모드 5 ✓
│  조향 권한   lidar
```

**`매니저 모드` 가 다르거나 `조향 권한` 이 `failsafe` 면 화면이 빨갛게 알려줍니다.**

## 1-b. S 코스만 연습할 때 — 전용 실행기

```bash
cd ~/T870_MCU && ./실행.sh
```

새 터미널:

```bash
cd ~/T870_MCU && source t870_env.sh
python3 tools/s_course_v1_0831.py
```

**5번 구간을 계속 발행하고, 라이다가 실제로 조향을 잡았는지 점검표로 보여줍니다.**
차는 몰지 않습니다 — 구동·조향은 라이다 회피 노드가 냅니다.

```
==============================================================
  S 코스 실행기 — '5' 번 구간을 /drive_mode 에 계속 발행
==============================================================
  ✅ 매니저 살아 있음
  ✅ 모드 5 (S 코스)
  ✅ 조향 권한 = lidar
  ✅ 회피 게이트 = true (열림)
  ✅ 라이다 조향 10.0 Hz
  ✅ 라이다 구동 10.0 Hz
  ✅ 안전 상태 OK
--------------------------------------------------------------
  전부 정상. 장애물을 놓고 회피가 도는지 보면 된다.
```

빨간 항목이 뜨면 **무엇을 고쳐야 하는지 그 자리에 적어줍니다.**

```
  🔴 조향 중앙 고정 (failsafe)
       권한자가 조향값을 안 보낸다. 차는 직진만 한다.
       라이다 노드가 떠 있는지, 토픽 이름·타입이 맞는지 확인.
```

### 🔴 5번에서 라이다가 조향·속도를 조절하려면 조건이 셋입니다

```
① 모드가 5            /drive_mode 에 "5"
② 조향 권한이 lidar    wheel_owner_overrides 의 "5:lidar"
③ 회피 게이트가 true   라이다가 /avoidance/active 에 true 발행
```

**셋 중 하나만 빠져도 차는 직진만 합니다.** 실행기 화면이 어느 것인지 알려줍니다.

#### 게이트는 중재기가 강제합니다

"5번 구간에서 `/avoidance/active` 가 true 일 때만 라이다가 조향과 속도를
조절할 수 있다" — 이 규칙을 **중재기가 직접 막습니다.**

```yaml
avoidance_gate_topic: "/avoidance/active"
avoidance_gate_modes: ["5"]
avoidance_gate_sources: ["lidar"]
avoidance_gate_timeout_s: 1.0
```

게이트가 닫히면 라이다의 **구동값과 조향값을 아예 없는 것으로** 취급합니다.

> **왜 라이다 자제에 안 맡기나**
> 라이다 노드에 버그가 있거나 옛 버전이 돌면 아무도 못 막습니다.
> 명령을 실제로 차에 내보내는 곳은 중재기뿐이라, 여기서 막아야 확실합니다.

> ⚠ **급정거(`/lidar_stop`)는 게이트와 무관하게 항상 받습니다.**
> 안전은 게이트보다 위입니다.

#### 신호가 없으면 막습니다

한 번도 못 받거나 1초 넘게 끊기면 **닫힌 것으로 봅니다.**
신호를 못 받는 상태에서 라이다가 차를 모는 것보다, 안 움직이는 편이 낫습니다.

`safety_state` 에 사유가 그대로 나옵니다:

| 값 | 뜻 |
|---|---|
| `AVOID_GATE(gate_false)` | 라이다가 false 를 보내는 중 |
| `AVOID_GATE(gate_no_signal)` | 신호를 한 번도 못 받았다 |
| `AVOID_GATE(gate_stale)` | 오다가 끊겼다 |

매니저 로그:

```
[INFO] 회피 게이트 열림 (모드 5) — lidar 의 구동·조향을 받는다
[WARN] 회피 게이트 닫힘 (모드 5, 사유 gate_no_signal) — lidar 의
       구동·조향을 받지 않는다. 급정거는 그대로 받는다.
```

#### 라이다팀이 할 것

회피 노드에서 **회피를 수행할 준비가 됐을 때** 발행하면 됩니다.

```python
pub = self.create_publisher(Bool, "/avoidance/active", 10)
...
m = Bool(); m.data = True      # 회피 로직이 살아 있을 때
pub.publish(m)                 # 4Hz 이상 (timeout 1초)
```

확인:

```bash
ros2 topic echo /avoidance/active
ros2 topic hz /avoidance/active
```

---

다른 구간을 연습하려면 번호만 바꾸면 됩니다.

```bash
python3 tools/s_course_v1_0831.py --mode 7      # T 주차
python3 tools/s_course_v1_0831.py --mode 10     # 평행 주차
```

토픽 이름이 다르면:

```bash
python3 tools/s_course_v1_0831.py \
  --lidar-wheel /lidar_wheel --lidar-drive /lidar_drive
```

---

## 2. 손으로 (mode_sim 없이)

```bash
cd ~/T870_MCU && source t870_env.sh
ros2 topic pub /drive_mode std_msgs/msg/String "{data: '5'}" -r 2
```

확인:

```bash
ros2 topic echo /mcu/current_mode          # 5 가 나와야 함
ros2 topic echo /mcu/active_wheel_source   # lidar 가 나와야 함
```

---

# 안 될 때

## ① 모드가 안 바뀐다

```bash
ros2 topic echo /mcu/current_mode
```

`0` 에서 안 움직이면:

| 원인 | 확인 |
|---|---|
| 토픽 이름 불일치 | `ros2 topic info /drive_mode` — 발행자·구독자가 각 1 이상인가 |
| known_modes 에 없는 값 | 매니저 로그에 `알 수 없는 모드 'XXX'` 가 뜨는지 |
| 매니저가 안 떠 있음 | `ros2 node list` |

> `unknown_mode_policy: "keep"` 이라 거부되면 **직전 모드가 그대로 유지됩니다.**
> 에러 없이 "안 바뀌는" 것처럼 보이니 로그를 꼭 보세요.

## ② 모드는 맞는데 조향이 안 된다

```bash
ros2 topic echo /mcu/active_wheel_source
```

| 값 | 뜻 |
|---|---|
| `lidar` / `camera` / `gps` | 정상. 그 소스가 조향 중 |
| `failsafe` | **권한자가 값을 안 준다. 조향 중앙 고정** |

`failsafe` 면 매니저가 이렇게 찍습니다:

```
[WARN] 조향이 중앙으로 고정된다 — 지금 모드 '5' 의 조향 권한자는
       'camera' 인데 그쪽에서 값이 안 온다 (camera:never_received).
  · 차는 구동은 하지만 조향은 안 한다. 직진만 한다.
  · 이 구간을 다른 센서로 돌리려면 yaml 의 wheel_owner_overrides 에
    '5:<소스>' 를 넣을 것.
```

**yaml 을 고쳤으면 반드시 다시 빌드하세요.**

```bash
cd ~/T870_MCU && colcon build --symlink-install
```

> `--symlink-install` 이어도 **yaml 은 다시 빌드해야 반영됩니다.**
> 파이썬 소스만 심볼릭 링크로 걸립니다.

## ③ 내 노드가 조향값을 쏘고 있나

```bash
ros2 topic hz /lidar_wheel
ros2 topic info -v /lidar_wheel     # 타입·QoS 확인
```

이름이 같아도 **타입이나 QoS 가 다르면 한 개도 안 들어옵니다. 에러도 안 납니다.**
매니저가 3초마다 검사해서 `[ERROR] ... 메시지 타입이 다르다` 로 알려줍니다.

## ④ 구동은 되는데 조향만 안 될 때

거의 항상 ②번입니다. **구동과 조향은 완전히 따로 결정됩니다.**

```
구동   고정 우선순위 (라이다 > 카메라 > GPS). 모드와 무관
조향   모드가 정한 권한자만. 다른 소스 값은 무시
```

---

# 카메라팀에게

같은 "일반 주행" 이라도 **번호가 다릅니다.**

```
1   출발 직선          4   교차로
6   교차로             8   좌회전
11  마지막 주행 (종점까지)
```

구간별 로직을 번호로 분기하면 됩니다.

```python
if mode == "1":     # 출발 직선
    ...
elif mode == "4":   # 첫 교차로
    ...
elif mode == "6":   # 두 번째 교차로
    ...
```

**4번과 6번이 서로 다른 번호라는 것이 이번 변경의 핵심입니다.**
이름 방식에서는 둘 다 `NORMAL` 이라 구분할 수 없었습니다.

---

# 관련 토픽

| 토픽 | 방향 | 내용 |
|---|---|---|
| `/drive_mode` | 입력 | 구간 번호 (String) |
| `/mcu/current_mode` | 출력 | 매니저가 받아들인 번호 |
| `/mcu/active_wheel_source` | 출력 | **지금 조향하는 소스** |
| `/mcu/active_drive_source` | 출력 | 지금 구동하는 소스 |
| `/mcu/safety_state` | 출력 | `OK` / `WHEEL_FALLBACK(...)` / `NO_DRIVE_SOURCE(...)` |

---

# 감사(audit)가 자동으로 막아주는 것

`tools/audit.py` 가 **도구와 yaml 이 어긋나면 설치할 때 잡습니다** (0831 추가).

```
🔴 mode_sim_v2_0831.py 의 기본 발행 토픽이 '/vehicle_mode' 인데
   매니저는 '/drive_mode' 를 구독한다. 모드를 눌러도 매니저가
   한 개도 못 받는다 (에러도 안 난다).

🔴 mode_sim_v2_0831.py 가 known_modes 에 없는 모드를 발행한다: ACCELERATION.
```

이번에 mode_sim 이 조용히 낡아 있던 것을 며칠 뒤에야 발견했기 때문에 넣었습니다.
