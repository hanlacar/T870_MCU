# 팀별 MCU 연동 점검표 (v25, 0901)

각 팀이 자기 노드를 우리 MCU에 붙일 때 **순서대로 치면서** 확인하는 문서입니다.
정상이면 어떤 답이 떠야 하는지, 안 뜨면 무엇이 문제인지까지 적었습니다.

> **가장 중요한 원칙**
> ROS 2에서 토픽이 안 통하는 이유 세 가지 — 발행자 없음, **메시지 타입 불일치**, **QoS 불일치** —
> 는 **전부 에러가 안 납니다.** `ros2 topic list`에는 멀쩡히 보입니다.
> 그래서 "떴으니까 됐겠지"로 넘어가면 안 되고, 아래 `ros2 topic info -v`로 실제 연결을 봐야 합니다.

---

## 0. 모든 팀 공통 — 먼저 이것부터

### 0-1. 도메인 확인 (제일 흔한 원인)

```bash
echo $ROS_DOMAIN_ID
```

**정상**: MCU를 띄운 터미널과 **똑같은 값**. 비어 있어도 되고 `77`이어도 되는데, **같아야** 합니다.

**다르면**: 서로 노드가 아예 안 보입니다. 에러도 경고도 없습니다.
0831에 라이다팀 SLAM이 하루 막힌 원인이 정확히 이것입니다.
같이 돌릴 모든 터미널에서 같은 값을 쓰세요.

```bash
unset ROS_DOMAIN_ID
```

### 0-2. MCU가 살아있는지

```bash
ros2 node list | grep mcu
```

**정상**:
```
/mcu_bridge
/mcu_manager
```

**둘 다 안 보이면**: MCU 담당이 `./실행.sh`를 안 띄웠거나 도메인이 다릅니다.
**`/mcu_manager`만 보이면**: 브릿지가 시리얼을 못 잡은 상태입니다(MCU 담당에게).

### 0-3. 아두이노 연결 상태

```bash
ros2 topic echo /mcu/connected --once
```

**정상**: `data: true`

**`false`면**: USB가 안 꽂혔거나 포트를 다른 프로그램이 잡고 있습니다.

```bash
ros2 topic echo /mcu/fw_state --once
```

**정상**: `data: READY` 또는 `data: ACTIVE`
**`ESTOP`이면**: 물리 E-Stop 버튼이 눌렸거나 배선이 끊겼습니다(NC 접점이라 단선도 눌린 것과 같습니다).

### 0-4. 지금 몇 번 구간인지

```bash
ros2 topic echo /mcu/current_mode --once
```

**정상**: `data: '0'` ~ `data: '11'` 중 하나 (문자열입니다)

**아무것도 안 나오면**: GPS팀 `route_follower`가 안 떠서 `/drive_mode`가 없는 상태입니다.
연습 중이라면 아래로 직접 구간을 지정하세요.

```bash
ros2 topic pub -r 5 /drive_mode std_msgs/msg/String "{data: '1'}"
```

### 0-5. 지금 누가 조향/구동 권한을 가졌는지

```bash
ros2 topic echo /mcu/active_wheel_source --once
```

**정상**: `camera` 또는 `lidar` — 지금 구간의 권한자
**`failsafe`면**: 권한자가 값을 안 보내고 있습니다. **차는 직진만 합니다.**

```bash
ros2 topic echo /mcu/safety_state --once
```

**정상**: `READY` / `ACTIVE`
**`ESTOP`**: E-Stop 래치. 해제는 `ros2 service call /mcu/reset_estop std_srvs/srv/Trigger '{}'`
**`HARD_STOP`**: 누군가 급정거를 걸고 있습니다.

---

## 1. 카메라팀

### 보내야 하는 것

| 토픽 | 타입 | 값 |
|---|---|---|
| `/camera_drive` | `std_msgs/msg/Float32` | -1.0, 0.0, 1.0, 2.0, 3.0 중 하나 |
| `/camera_wheel` | `std_msgs/msg/Int32` | -27 ~ +27 [도] (+가 우측) |
| `/camera_stop` | `std_msgs/msg/Bool` | 정지선·적색신호에서 true |

**담당 구간**: 1, 2, 3, 4, 6, 8, 9, 11 (그리고 5번에서 회피가 꺼져 있는 동안)

### 점검

```bash
ros2 topic info -v /camera_drive
```

**정상** — 발행 1(카메라), 구독 1(mcu_manager), 타입과 QoS가 같아야 합니다:
```
Type: std_msgs/msg/Float32
Publisher count: 1
  Node name: camera_pixel_controller
  ...
  Reliability: RELIABLE
Subscription count: 1
  Node name: mcu_manager
  ...
  Reliability: RELIABLE
```

**`Subscription count: 0`이면**: MCU가 안 떴거나 도메인이 다릅니다.
**`Type`이 다르면**: 우리는 `Float32`를 기다립니다. `Int32`로 쏘면 **한 개도 안 들어옵니다.**
**`Reliability`가 서로 다르면**(BEST_EFFORT vs RELIABLE): 역시 한 개도 안 들어옵니다.

```bash
ros2 topic info -v /camera_wheel
```
**정상**: `Type: std_msgs/msg/Int32`, 발행 1 / 구독 1

```bash
ros2 topic info -v /camera_stop
```
**정상**: `Type: std_msgs/msg/Bool`, 발행 1 / 구독 1

### 실제로 MCU가 받고 있는지

```bash
ros2 topic pub -r 5 /drive_mode std_msgs/msg/String "{data: '1'}"
```
(다른 창에서)
```bash
ros2 topic echo /mcu/active_wheel_source --once
```
**정상**: `data: camera`
**`failsafe`면**: `/camera_wheel`이 0.5초 안에 안 오고 있습니다. 발행 주기를 2Hz 이상으로.

```bash
ros2 topic echo /mcu/cmd_drive --once
```
**정상**: 카메라가 보낸 값과 같은 숫자
**`0.0`만 나오면**: 값이 거부됐습니다. `-1, 0, 1, 2, 3` 외의 값(예: 1.5)을 보내면 거부됩니다.

### 급정거 확인

```bash
ros2 topic echo /mcu/hard_stop_active
```
`/camera_stop`에 true를 쏘는 순간 `data: true`로 바뀌어야 합니다.
바뀌지 않으면 타입이나 토픽 이름이 틀린 것입니다.

### 🔴 카메라팀에 지금 고쳐야 할 것

`imu_manager`가 `/vehicle_mode`를 **String**으로 구독하는데, GPS팀은 **Int32**로 발행합니다.
**한 개도 못 받고 있습니다.** 확인:

```bash
ros2 topic info -v /vehicle_mode
```

`Type`이 발행/구독에서 다르게 보이면 그 문제입니다.
**해결**: `imu_manager`의 `mode_topic`을 `/mcu/current_mode`(String, 번호)로 바꾸세요.

---

## 2. 라이다팀 (회피 · S코스)

### 보내야 하는 것

| 토픽 | 타입 | 값 |
|---|---|---|
| `/lidar_drive` | `std_msgs/msg/Float32` | -1.0 ~ 3.0 |
| `/lidar_wheel` | `std_msgs/msg/Int32` | -27 ~ +27 |
| `/lidar_stop` | `std_msgs/msg/Bool` | 장애물 급정거 |
| `/avoidance/active` | `std_msgs/msg/Bool` | **회피 중일 때만 true** |

**담당 구간**: 5번(회피 중), 7·10번(주차)

### 5번 S코스는 규칙이 다릅니다

평소에는 **카메라**가 조향합니다. `/avoidance/active`가 **true인 동안만** 라이다가 가져갑니다.
false가 되거나 1초 넘게 안 오면 **즉시 카메라로 돌아갑니다.**

```bash
ros2 topic hz /avoidance/active
```
**정상**: `average rate: 10.0` 정도 (최소 4Hz 이상이어야 합니다)
**`no new messages`면**: 게이트가 닫혀서 5번에서 라이다가 조향을 못 잡습니다.

### 점검

```bash
ros2 topic info -v /lidar_drive
```
**정상**: `Type: std_msgs/msg/Float32`, 발행 1 / 구독 1(mcu_manager)

```bash
ros2 topic pub -r 5 /drive_mode std_msgs/msg/String "{data: '5'}"
```
(다른 창)
```bash
ros2 topic echo /mcu/active_wheel_source
```
**정상**: `/avoidance/active`가 true인 동안 `lidar`, false면 `camera`로 바뀝니다.
**계속 `camera`면**: 게이트가 안 열립니다. 위 `topic hz`를 확인하세요.
**계속 `failsafe`면**: 카메라도 라이다도 조향값을 안 보내는 상태입니다.

### 🔴 발행자가 둘이면 안 되는 것

```bash
ros2 topic info /lidar_drive
```
**정상**: `Publisher count: 1`

**2 이상이면**: `avoidance_sim`, `Lidar_ws`, `t_parking_*` 중 두 개 이상이 동시에 떠 있습니다.
셋 다 같은 토픽을 쓰므로 **한 번에 하나만** 띄우세요.

### SLAM이 안 될 때

```bash
ros2 run tf2_ros tf2_echo odom base_link
```
**정상**: 차를 안 움직여도 값이 계속 나옵니다(정지 상태면 0). 차를 밀면 숫자가 변합니다.
**`frame does not exist`면**: 도메인이 다르거나 브릿지가 안 떠 있습니다.

```bash
ros2 topic info /odom
```
**정상**: `Publisher count: 1`
**2 이상이면**: 미션팀 `wheel_odom_node`나 주차팀 `mcu_odom_adapter.py`가 아직 켜져 있습니다. **꺼야 합니다.**

```bash
ros2 launch t870_mcu t870_frames.launch.py
```
**정상**: 오류 없이 뜨고, 아래로 센서 위치가 확인됩니다.
```bash
ros2 run tf2_ros tf2_echo base_link front_laser
```
**정상**: `Translation: [0.730, 0.000, 0.105]`

---

## 3. 주차팀 (T주차 · 평행주차)

### 보내야 하는 것

7·10번 구간에서는 **`lidar` 소스로** 보냅니다. 별도 주차 토픽은 쓰지 않습니다.

| 토픽 | 타입 |
|---|---|
| `/lidar_drive` | `std_msgs/msg/Float32` |
| `/lidar_wheel` | `std_msgs/msg/Int32` |
| `/lidar_stop` | `std_msgs/msg/Bool` |

### 점검

```bash
ros2 topic pub -r 5 /drive_mode std_msgs/msg/String "{data: '7'}"
```
(다른 창)
```bash
ros2 topic echo /mcu/active_wheel_source --once
```
**정상**: `data: lidar` (7·10번은 게이트 없이 라이다가 바로 권한자입니다)

### 🔴 주차팀이 지금 고쳐야 할 것 두 가지

**하나 — 축거를 0.77 → 0.73으로.**

0901 실측으로 확정됐습니다. 바퀴 반지름 0.135(지름 0.270), 바퀴 바깥 끝에서 끝까지 1.000 m
→ 축거 = 1.000 − 0.270 = **0.730 m**. 기존 실측값과 정확히 일치합니다.

`turtle_car.urdf.xacro`, `nav2_params`의 `reverse_wheel_base`, `auto_t_parking`, bench yaml 등
**20곳 이상**에 0.77이 있습니다. 4 cm는 최소회전반경 5.5% 오차라 후진 주차 경로가 어긋납니다.

```bash
grep -rn "0.77" ~/t_parking_ws/src | grep -i wheel
```
**정상**: 아무것도 안 나와야 합니다.

**둘 — `mcu_odom_adapter.py`를 끄세요.**

이제 MCU가 `/odom`과 `odom→base_link` TF를 직접 발행합니다.
어댑터가 같이 켜져 있으면 두 노드가 같은 TF를 쏘고, **RViz에서 차가 튑니다.**

```bash
ros2 node list | grep -i odom
```
**정상**: `mcu_odom_adapter`가 보이지 않아야 합니다.

### 참고 — 라이다 위치가 그쪽 URDF와 거의 같습니다

주차팀 URDF의 `front_laser +0.7225` / `rear_laser −0.6825` 와
우리 실측 `+0.730` / `−0.680`이 각각 0.75 cm, 0.25 cm 차이입니다.
서로 모르고 잰 값이 이만큼 가까우니 둘 다 신뢰할 만합니다.

---

## 4. GPS · 미션팀

### 보내야 하는 것

| 토픽 | 타입 | 값 |
|---|---|---|
| `/drive_mode` | `std_msgs/msg/String` | **"0" ~ "11" 번호** |
| `/gps_drive` | `std_msgs/msg/Float32` | -1.0 ~ 3.0 |
| `/gps_wheel` | `std_msgs/msg/Int32` | -27 ~ +27 |

### 점검

```bash
ros2 topic echo /drive_mode --once
```
**정상**: `data: '4'` 같은 **번호**

**`data: 'INTERSECTION'` 처럼 이름이면**: 아직 옛 방식입니다. MCU가 별칭표로 번호로 바꿔주지만,
**이름 6종으로는 구간 11개를 구분할 수 없습니다.** 특히 정지선/교차로가 4번과 6번 두 군데인데
`INTERSECTION` 하나뿐이라 지금은 4번으로만 갑니다.

MCU 로그에 이런 줄이 뜨면 별칭이 쓰이고 있다는 뜻입니다:
```
[INFO] 모드 별칭: 'INTERSECTION' → '4' 로 받는다.
```

**해결**: `gps_route_follower_node.py:1225`의 `String(data=output.mode)`에
이름 대신 번호를 넣으면 됩니다. CSV의 `mode` 컬럼을 번호로 기록하는 것이 가장 깔끔합니다.

### 🔴 미션팀이 지금 고쳐야 할 것

**하나 — `wheel_odom_node`를 끄세요.**

MCU가 `/odom`과 TF를 직접 발행합니다. 같이 켜면 충돌합니다.

```bash
ros2 topic info /odom
```
**정상**: `Publisher count: 1`

**둘 — `/vehicle_mode` 발행을 정리하세요.**

`Int32`로 쏘는데 카메라팀 `imu_manager`는 `String`으로 구독합니다. 아무도 못 받습니다.
카메라팀이 `/mcu/current_mode`로 옮기면 `/vehicle_mode`는 더 이상 필요 없습니다.

### 참고 — GPS 안테나 위치

```bash
ros2 run tf2_ros tf2_echo base_link gps_link
```
**정상**: `Translation: [0.450, 0.000, 0.430]`
(원점은 네 바퀴 정중앙, 지면. 안테나는 정중앙보다 45 cm 앞, 지면에서 43 cm)

---

## 5. 전체 통합 점검 (다 같이 띄웠을 때)

### 5-1. 최종 명령이 하나만 나가는지

```bash
ros2 topic info /mcu/cmd_drive
```
**정상**: `Publisher count: 1` (mcu_manager), `Subscription count: 1` (mcu_bridge)

**2 이상이면**: 누군가 중재기를 건너뛰고 직접 쏘고 있습니다. **위험합니다.** 찾아서 꺼야 합니다.

### 5-2. 오돔·TF 소유자가 하나인지

```bash
ros2 topic info /odom
```
**정상**: `Publisher count: 1`

```bash
ros2 run tf2_tools view_frames
```
**정상**: `map → odom → base_link → {front_laser, rear_laser, laser, camera_link → imu_link, gps_link}`
한 그루의 나무여야 합니다. 두 조각으로 갈라져 있으면 SLAM이 스캔을 전부 버립니다.

### 5-3. MCU가 이상을 감지하면 로그로 말해줍니다

브릿지·매니저 터미널에 이런 줄이 뜨면 바로 조치하세요.

| 로그 | 뜻 | 조치 |
|---|---|---|
| `/odom 를 다른 노드도 발행 중이다: ...` | 오돔 발행자 둘 이상 | 그 노드를 끄세요 |
| `... 를 다른 mcu_bridge 가 이미 쓰고 있다` | 브릿지 두 개 | 하나만 띄우세요 |
| `타입 불일치` | 메시지 타입이 다름 | 발행 쪽 타입 수정 |
| `QoS 불일치` | RELIABLE vs BEST_EFFORT | 발행 쪽 QoS 수정 |
| `조향이 중앙으로 고정된다 — 권한자는 'camera'인데 값이 안 온다` | 권한자가 조용함 | 그 팀 노드 확인 |
| `회피 게이트 닫힘 (모드 5, 사유 gate_no_signal)` | `/avoidance/active` 미수신 | 라이다팀 확인 |
| `모드 별칭: 'S_COURSE' → '5' 로 받는다` | GPS가 아직 이름 발행 | 번호로 전환 요청 |
| `ROS E-STOP 래치됨 ... 발행자: /어떤노드` | E-Stop이 걸림 | 그 노드가 왜 쐈는지 확인 |

### 5-4. 한눈에 보는 감시 도구

```bash
cd ~/mcu_ws && python3 tools/team_monitor.py
```
지금 어떤 소스가 살아있고 누가 권한을 잡았는지 한 화면에 보여줍니다.

### 5-5. 환경 점검

```bash
cd ~/mcu_ws && ./실행.sh 점검
```
도메인, 시리얼 포트 권한, 빌드 상태, 패키지 중복을 한 번에 확인합니다.

---

## 6. 아직 못 믿는 것 — 오돔

이 차는 **4WD이고 앞·뒤축 모두 오픈 디퍼렌셜**입니다. 구동하지 않는 바퀴가 없어서,
접지가 나쁜 쪽으로 토크가 몰리면 그 바퀴가 헛돕니다. 엔코더는 구동모터에 달려
좌우 평균을 세므로 헛도는 만큼 거리를 더 셉니다. **출발 순간에 특히 큽니다.**

0830 실측: 같은 명령 15초인데 간 거리가 **1.73 m / 4.98 m / 6.32 m** 로 3.6배 차이났습니다.

- `/mcu/encoder` — 바퀴 회전량으로는 믿어도 됩니다
- `/mcu/distance_m` — "바퀴가 굴러간 거리"이지 지면 이동거리가 아닙니다
- `/odom` — **위치추정의 주 소스로 쓰지 마세요.** SLAM의 초기 추정값 정도로만

맵이 이상하면 오돔을 먼저 의심하세요. 근본 해결은 라이다 스캔매칭 오돔이나
구동하지 않는 다섯 번째 바퀴이고, 라이다팀과 논의 중입니다.
