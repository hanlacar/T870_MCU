# 라이다팀 SLAM 이 안 되던 이유 — 0831

`Message Filter dropping message ... queue is full` 로 스캔이 전부 버려지던 건
**MCU 고장이 아니었습니다.** 원인은 두 개고, 하나는 그쪽 터미널 설정, 하나는 우리 기본값입니다.

---

## 1. (주원인) ROS_DOMAIN_ID 가 서로 달랐습니다

우리 `./실행.sh` 는 `t870_env.sh` 를 읽는데, 거기에 `ROS_DOMAIN_ID=77` 이 박혀 있었습니다.
그런데 라이다·static TF·slam_toolbox·Claude Code 터미널은 `t870_env.sh` 를 안 읽어서
**전부 0** 으로 떴습니다.

ROS 2 는 도메인이 다르면 노드끼리 **존재조차 모릅니다. 에러도 경고도 없습니다.**
그래서 이렇게 보였습니다.

| 보였던 증상 | 도메인 불일치로 설명되나 |
|---|---|
| `ros2 param get /mcu_bridge ...` → Node not found | ✅ 0 번 도메인에서는 안 보임 |
| `ros2 topic hz /odom` → 발행 안 됨 | ✅ 같은 이유 (+ 3번 항목도 참고) |
| `ros2 topic echo /mcu/encoder` → 발행 안 됨 | ✅ 같은 이유 |
| slam 이 `odom→base_link` TF 를 못 받음 | ✅ 브릿지는 77, slam 은 0 |
| 라이다·static TF·slam 셋은 서로 잘 보임 | ✅ 셋 다 0 이라 자기들끼리는 정상 |

### "매니저는 살아있고 브릿지만 죽었다" 는 추론도 틀렸습니다

그건 `ros2 node list` 가 아니라 터미널 1 화면 로그를 보고 판단한 것입니다.
브릿지는 연결이 정상이면 `포트 열림`, `시리얼 준비 완료` 두 줄만 찍고 조용해집니다.
반면 매니저는 조향 경고를 계속 뱉습니다. 그 화면은 **브릿지가 멀쩡할 때의 정상 모습**입니다.

코드로도 그렇습니다. 브릿지는 포트를 못 찾거나 점유당해도 죽지 않고 0.2 초마다 계속
재시도합니다(`_try_open`). 시리얼 문제로 노드가 사라지는 일은 거의 없습니다.

### 고쳤습니다 — v23 부터 `t870_env.sh` 는 도메인을 **강제하지 않습니다**

지금은 팀마다 다른 값으로 실험하는 단계라, 특정 값을 몰래 export 하는 쪽이 더 위험합니다.
그래서 규칙을 뒤집었습니다.

* 셸에 이미 `ROS_DOMAIN_ID` 가 있으면 → 그대로 존중합니다
* 없으면 → 손대지 않습니다 (ROS 기본값 0)

즉 `t870_env.sh` 를 source 해도 이제 도메인이 바뀌지 않습니다.
**대신 같이 돌릴 터미널은 전부 같은 값이어야 합니다.**

맞추는 법 — 함께 돌릴 모든 터미널에서 같은 값을 씁니다. 실험 단계에는 그냥 0 이 편합니다.

```bash
unset ROS_DOMAIN_ID
```

특정 값으로 묶고 싶으면 모든 터미널에서 똑같이:

```bash
export ROS_DOMAIN_ID=77
```

확인:

```bash
echo $ROS_DOMAIN_ID
```

```bash
ros2 node list | grep mcu
```

`/mcu_bridge` 와 `/mcu_manager` 가 둘 다 보여야 합니다.

---

## 2. (우리 잘못) `publish_tf` 기본값이 `false` 였습니다

`config/t870_mcu.yaml` 의 기본값이 `false` 라서, 도메인을 맞춰도 TF 가 안 나갔을 겁니다.
그쪽에서 각자 `true` 로 고쳐 쓰고 계셨는데, 그러면 다음에 우리 배포본을 받는 순간 도로 꺼집니다.

**v23 부터 기본값을 `true` 로 바꿨습니다.** 이제 그냥 받아서 쓰면 됩니다.

---

## 3. `/odom` 은 원래 우리가 안 쐈습니다 — 고장이 아닙니다

우리가 쏘는 이름은 **`/mcu/odom`** 입니다. `/odom` 은 미션팀 `wheel_odom_node` 와
주차팀 `mcu_odom_adapter.py` 가 `/mcu/odom` 을 받아 이름만 바꿔 다시 쏘던 것입니다.

그러니 `ros2 topic hz /odom` 이 안 나오는 건 정상이고, 진단 기준으로 쓰면 안 됩니다.
**그리고 slam_toolbox 는 `/odom` 토픽을 구독하지 않습니다.** 오직 TF `odom→base_link`
만 봅니다. 위 1·2 번만 해결되면 SLAM 은 됩니다.

`/odom` 을 우리가 직접 쏘는 옵션(`pub_topic_odom_alias`)을 넣어 두긴 했는데,
**기본은 꺼져 있습니다.** 켜려면 미션팀·주차팀의 재발행 노드를 먼저 꺼야 합니다.
셋이 동시에 쏘면 구독자가 어느 걸 받을지 알 수 없습니다.

---

## 4. 확인 순서

```bash
echo $ROS_DOMAIN_ID
```
```bash
ros2 node list | grep mcu
```
```bash
ros2 topic hz /mcu/odom
```
```bash
ros2 run tf2_ros tf2_echo odom base_link
```

마지막 것에서 값이 나오고 차를 밀면 숫자가 변하면 성공입니다.
그 다음 slam 창에서 `queue is full` 이 사라졌는지 보세요.

---

## 5. 그래도 알아두셔야 할 것 — 이 오돔은 아직 못 믿습니다

이 차는 **4WD 이고 앞·뒤축 모두 오픈 디퍼렌셜**입니다 (0831 확정).
"구동하지 않는 바퀴" 가 없어서, 접지가 나쁜 쪽 바퀴로 토크가 몰리면 그 바퀴가 헛돕니다.
엔코더는 앞축 중앙에서 좌우 **평균** 을 세므로 헛도는 만큼 거리를 더 셉니다.
출발 순간에 특히 큽니다.

선회 기하 보정은 v23 에 함수로 넣어 뒀지만, 앞축 중앙 엔코더에는 필요 없습니다
(평균이 이미 축 중심값이라 `cos(δ)` 만으로 정확합니다). 만약 엔코더가 한쪽 바퀴
허브에 달린 것으로 확인되면 `encoder_mount: single_wheel` 로 바꾸고 윤거를 넣습니다.

**차동 슬립은 어떤 계산으로도 못 없앱니다.** slam_toolbox 의 스캔매칭이 어느 정도
보정해 주지만, 초기 추정이 튀면 정합이 깨집니다. **맵이 이상하면 오돔을 먼저
의심하세요.** 근본 해결은 라이다 스캔매칭 오돔(`rf2o_laser_odometry`)을 주 소스로
쓰거나, 구동하지 않는 다섯 번째 바퀴를 다는 것입니다. 이건 따로 논의합시다.
