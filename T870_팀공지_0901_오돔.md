# 팀 공지 — 오돔이 바뀝니다 (v24, 0901)

두 가지가 바뀝니다. **둘 다 다른 팀 코드에 영향이 있습니다.** 읽고 각자 확인해 주세요.

---

## 1. 오돔 기준점이 뒤축 중심 → **차량 정중앙** 으로 옮겨졌습니다

`base_link` 의 원점 정의가 바뀌었습니다.

* 이전: 뒤축 중심, 지면 높이
* 지금: **네 바퀴 정중앙, 지면 높이**
* 옮긴 거리: 축거의 절반 = 0.73 / 2 = **0.365 m 앞**

### 무엇이 달라지나

**`/odom` 의 위치가 진행 방향으로 0.365 m 앞을 가리킵니다.**
정지 상태에서도 그렇습니다. 좌표계 자체가 옮겨진 것이지 차가 움직인 게 아닙니다.

**`twist.linear.y` 가 이제 0 이 아닙니다.**
중심점은 차가 회전할 때 옆으로 쓸립니다. `vy = (L/2) × wz` 입니다.
예: 0.5 m/s 로 20도 조향이면 `wz = 0.249 rad/s`, `vy = 0.091 m/s`.
`vy = 0` 을 가정한 필터나 계산이 있으면 고쳐야 합니다.

### 여러분이 할 일

**TF 를 써서 좌표를 변환하고 있다면 → 할 일 없습니다.**
`t870_frames.yaml` 의 센서 위치도 같이 0.365 옮겼기 때문에 자동으로 맞습니다.
(camera_link 1.10→0.735, laser 1.05→0.685, gps_link 0.20→−0.165)

**좌표를 코드에서 손으로 더하고 빼고 있다면 → 알려주세요.**
그런 코드가 있으면 이번에 어긋납니다. 같이 확인합시다.

> ⚠ `odom_reference: "center"` 와 `t870_frames.yaml` 의 x 값은 **한 쌍** 입니다.
> 둘 중 하나만 되돌리면 센서가 본 것이 통째로 0.365 m 어긋납니다.

---

## 2. `/odom` 은 이제 **MCU 브릿지가 직접** 발행합니다

지금까지는 브릿지가 `/mcu/odom` 을 쏘고, 그걸 받아서 두 노드가 `/odom` 으로
다시 쏘고 있었습니다. 이제 원천에서 한 번만 쏩니다.

### 반드시 꺼 주세요 — 안 끄면 충돌합니다

| 팀 | 노드 | 조치 |
|---|---|---|
| 미션 | `mission_manager/wheel_odom_node` | 실행 목록에서 제외 (또는 `publish_tf: false` + 발행 중단) |
| 주차 | `t_parking_sim/scripts/mcu_odom_adapter.py` | 실행 목록에서 제외 |

**셋이 같은 `/odom` 과 같은 `odom→base_link` TF 를 쏘면 구독자는 어느 걸 받을지
알 수 없습니다. 에러도 경고도 안 납니다.** RViz 에서 차가 튀거나 SLAM 정합이
깨지는 형태로만 나타나서 원인 찾기가 아주 어렵습니다.

`/odom` 을 **구독** 하던 코드는 그대로 두면 됩니다. 이름도 타입도 안 바뀝니다.
(회피팀 `safety_node`, `coordinator_node`, `route_follower` 가 여기 해당합니다)

### 확인 방법

```bash
ros2 topic info /odom
```

`Publisher count: 1` 이어야 합니다. 2 이상이면 아직 안 끈 노드가 있습니다.

```bash
ros2 run tf2_tools view_frames
```

`odom → base_link` 를 쏘는 주체가 하나여야 합니다.

---

## 3. 그래도 이 오돔은 아직 못 믿습니다

원점 정리는 "**어디**를 기준으로 하나" 를 맞춘 것이고, "**얼마나 맞나**" 는 그대로입니다.

이 차는 4WD 이고 앞·뒤축 모두 오픈 디퍼렌셜입니다. 구동하지 않는 바퀴가 없어서,
접지가 나쁜 쪽으로 토크가 몰리면 그 바퀴가 헛돕니다. 엔코더는 구동모터에 달려
좌우 평균을 세므로 헛도는 만큼 거리를 더 셉니다. 출발 순간에 특히 큽니다.

**위치추정의 주 소스로 쓰지 마세요.** 지금은 SLAM 의 초기 추정값 정도로만
쓰는 게 맞습니다. 근본 해결은 라이다 스캔매칭 오돔(`rf2o_laser_odometry`)을
주 소스로 쓰거나 구동하지 않는 다섯 번째 바퀴를 다는 것이고, 라이다팀과
방향을 정하는 중입니다.

---

## 4. 센서 위치 — 실측 완료 (0901)

`t870_frames.yaml` 의 값은 전부 실측입니다. 추정치는 없습니다.

```
바퀴 반지름 0.135 m (지름 0.270)
앞바퀴~뒷바퀴, 바퀴 바깥 끝 기준 1.000 m
  → 축거 = 1.000 − 2 × 0.135 = 0.730 m
앞 윤거 0.775 / 뒤 윤거 0.785   (뒤가 1 cm 넓음, 오른쪽이 더 나옴)

원점 = 네 바퀴 정중앙, 지면 (= 바퀴 바깥 1.000 m 직사각형의 중심)

front_laser   x +0.730   y 0   z 0.105   yaw 0        (앞을 봄)
rear_laser    x −0.680   y 0   z 0.155   yaw 3.14159  (뒤를 봄)
laser         front_laser 와 동일 (구버전 이름 호환용)
camera_link   x +0.015   y 0   z 0.970   pitch +0.0873 (아래로 5도)
gps_link      x +0.450   y 0   z 0.430
```

**축거는 0.730 m 입니다.** 바퀴 바깥 치수에서 계산한 값이 기존 실측 0.73 과
정확히 일치했습니다. 주차팀 코드의 `wheel_base: 0.77` (URDF, nav2_params
`reverse_wheel_base`, auto_t_parking, bench yaml 등 20곳 이상) 을
**0.73 으로 통일해 주세요.** 4 cm 차이는 최소회전반경 5.5% 오차입니다.

라이다 위치는 주차팀 URDF 의 독립 측정값(front +0.7225 / rear −0.6825) 과
각각 0.75 cm, 0.25 cm 차이로 교차 검증됐습니다.

**라이다 프레임 이름은 `front_laser` / `rear_laser`** 입니다 (라이다팀 규약).
구버전 `laser` 도 전방 라이다와 같은 자리로 함께 발행하므로,
아직 `frame_id=laser` 로 뜨는 스크립트가 있어도 깨지지 않습니다.

---

## 5. 이번에 같이 들어간 안전 장치 세 가지

**시리얼 포트 배타 잠금.** 브릿지가 두 개 뜨면 둘 다 같은 포트를 열어
명령을 번갈아 쏘고 STATUS 를 반씩 나눠 받습니다. "가끔 명령이 씹힌다" 로만
보여서 원인 찾기가 어렵습니다. 이제 먼저 잡은 쪽만 쓰고, 나중 것은
로그로 알리고 연결하지 않습니다.

**오돔 중복 발행자 감시.** 5초마다 `/odom` 과 `/mcu/odom` 의 발행자를 확인해
우리 말고 다른 노드가 있으면 노드 이름과 함께 크게 알립니다.

**모드 전환 시 이전 구간 명령 무효화.** 7번(T주차) → 8번으로 넘어갈 때
라이다가 마지막에 보낸 큰 조향각이 타임아웃(0.5초) 전까지 살아 있어
새 구간 출발에 그대로 먹히던 문제를 막습니다. 급정거는 무효화하지 않습니다.

